server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

app:
  jwt:
    secret: "change_this_in_prod_very_long_secret_key_please"
    expiration-ms: 604800000 # 7 days